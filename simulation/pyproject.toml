[project]
name = "simulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "juan-tamarit", email = "juan.tamarit@outlook.es" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
simulation = "simulation:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
